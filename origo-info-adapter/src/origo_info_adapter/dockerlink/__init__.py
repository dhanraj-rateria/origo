"""The Docker device-loop mock link. Not part of the public package surface (see
../__init__.py's __all__) — reached only through build_adapter(), the same way
.fake and .stellarstation already are."""
